# 项目配置（仅本地，不提交）
project.toml
AGENT.md
test_game.bat
test_game_menu.bat

# Agent launchers and run logs live under tools/ (the whole tools/ tree is local only):
#   tools/run/_launch_client.bat     test client, started through explorer.exe
#   tools/run/_run_server*.{bat,ps1,vbs}   headless dev server
#   tools/logs/*.log                 run logs

# Agent scratch (regenerate on demand, never commit)
_*.py
_*.ps1
_*.log
_*.bat
_*.png
_preview/
_cls/

# Gradle / Forge
.gradle/
build/
out/
run/
runs/

# Build toolchain (local-only, not tracked)
build.gradle
settings.gradle
gradle.properties
gradlew
gradlew.bat
gradle/


# IDE
.idea/
.vscode/
*.iml
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db

# Dev tools (local-only, not tracked)
tools/
# local helper scripts (never shipped)
_*.vbs
